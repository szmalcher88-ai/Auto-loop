# Plan zadań pętli

<!-- Format: jedna linia = jedno zadanie = jeden commit.
     - [ ]  otwarte (pętla weźmie pierwsze z góry)
     - [x]  zrobione (oznacza PĘTLA po zielonej weryfikacji — nie człowiek, nie agent)
     - [!]  eskalowane (czeka na człowieka; po naprawie zmień ręcznie na [ ])

     Granulacja: zadanie musi być (a) wykonalne bez zadawania pytań,
     (b) weryfikowalne przez verify_commands, (c) mniejsze niż max_diff_lines.
     "Dodaj walidację emaila w formularzu rejestracji" — dobre.
     "Popraw moduł auth" — złe (niejednoznaczne, niemierzalne, za szerokie).

     Ten plik należy do pętli. Agent ma zakaz go dotykać (guard automatyczny). -->

- [ ] Przykładowe zadanie 1: utwórz moduł greet.py z funkcją greet(name) zwracającą "Hello, {name}!"
- [ ] Przykładowe zadanie 2: dodaj do greet.py funkcję farewell(name) zwracającą "Bye, {name}!"
